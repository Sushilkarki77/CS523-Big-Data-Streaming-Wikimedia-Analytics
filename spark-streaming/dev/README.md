# Development Spark job

`wiki_recentchange_console.scala` — Phase 3 console validation (Kafka → windows → `show()`). The **production pipeline** uses `wiki_recentchange_hive/` instead.

Run:

```bash
bash scripts/dev/run-spark-streaming-console.sh
```
